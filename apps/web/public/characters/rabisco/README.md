# Assets do Rabisco (placeholder)

Os PNGs em `actions/` e `base/` são **placeholders de 1x1 pixel transparente**
— servem só para o pipeline (worker → Remotion) rodar ponta a ponta sem
quebrar por arquivo ausente. Substitua pelos PNGs finais do personagem
(fundo transparente, personagem centralizado, mínimo 1200x1600px) antes de
publicar qualquer vídeo real. Ver `RABISCO.md` §3 na raiz do repo para a
lista completa de arquivos esperados e requisitos técnicos.
